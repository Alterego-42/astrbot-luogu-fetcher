# AstrBot 洛谷助手插件

一款基于 Playwright 的洛谷（Luogu） AstrBot 插件，支持账号绑定、自动打卡、数据爬取、热度图截图、题库跳转等功能。

## 功能特性

| 指令 | 功能 |
|------|------|
| `/luogu bind <手机号> <密码>` | 绑定洛谷账号（支持 ddddocr 自动识别验证码） |
| `/luogu info` | 查看个人统计卡片 |
| `/luogu info -f` | 强制从洛谷重新拉取数据 |
| `/luogu checkin` | 每日打卡截图 |
| `/luogu heatmap` | 做题热度图（洛谷原版截图） |
| `/luogu elo` | 比赛等级分趋势图 |
| `/luogu practice` | 练习情况（按难度分类） |
| `/luogu practice -f` | 强制重新获取练习数据 |
| `/luogu jump` | 题库跳转（多轮交互选题） |
| `/luogu help` | 显示帮助信息 |

### 可获取的数据

- **个人主页**: 用户名、UID、排名、Rating、通过数、提交数
- **咕值分解**: 基础信用、练习分、比赛分、社区贡献、成就
- **评定比赛**: 参与评定的比赛名称与日期
- **做题热度**: 近 26 周每日做题数和最大难度
- **题目列表**: 已通过题目，按难度分类（暂无评定/入门/普及−/普及/提高−/普及+/提高/提高+/省选−/省选/NOI−/NOI/NOI+/CTSC）

## 安装

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

主要依赖：
- `playwright` - 网页自动化
- `matplotlib` - 图表生成
- `ddddocr` - 验证码识别（可选，未安装时跳过自动识别）

安装 Playwright 浏览器：
```bash
playwright install chromium
```

### 2. 安装插件

将插件目录放入 AstrBot 的插件目录即可。

## 使用方法

### 绑定账号

私聊发送（保护密码安全）：
```
/luogu bind 你的手机号 你的密码
```

可选参数 `-s` 或 `--save`：保存账号密码到本地（加密存储），后续如 Cookie 过期可自动重新登录：
```
/luogu bind 你的手机号 你的密码 -s
```

> ⚠️ 账号密码仅保存在本设备（已加入 `.gitignore`），不会上传到仓库。

绑定成功后会自动获取并缓存个人主页和练习数据。

### 查看统计

```
/luogu info          # 使用缓存数据
/luogu info -f       # 强制从洛谷重新拉取
```

返回包含以下内容：
- 通过数 / 提交数 / 等级分 (Rating)
- 咕值 (CSR) 及其构成明细
- 排名、评定比赛数
- 评定比赛名称列表

### 每日打卡

```
/luogu checkin
```

返回洛谷打卡页面截图，包含连续打卡天数、今日运势等信息。

### 做题热度图

```
/luogu heatmap
```

返回洛谷原版热度图截图，每格颜色代表当天通过的最大难度（灰→红→橙→黄→绿→蓝→紫→黑）。

### 等级分趋势

```
/luogu elo
```

返回比赛等级分趋势折线图，标注每场分数及变化量。

### 练习情况

```
/luogu practice          # 使用缓存数据
/luogu practice -f       # 强制重新获取
```

返回按难度分类的已通过题目数量，并附难度分布卡片图。

### 题库跳转

```
/luogu jump
```

多轮交互式题库筛选与跳转，支持以下筛选条件：

**难度筛选**：
| 输入 | 难度 |
|------|------|
| `0` | 暂无评定 |
| `1` | 入门 |
| `2` | 普及− |
| `3` | 普及/提高− |
| `4` | 普及+/提高 |
| `5` | 提高+/省选− |
| `6` | 省选/NOI− |
| `7` | NOI/NOI+/CTSC |

**标签筛选**（支持模糊匹配）：
- 算法标签：`KMP`、`DP`、`图论`、`贪心` 等
- 来源标签：`ICPC`、`CSP`、`Codeforces`、`AtCoder`、`NOI` 等
- 时间标签：`2020`、`2021`、`2022` 等
- 区域标签：`上海`、`北京`、`浙江` 等
- 特殊题目标签：`O2优化`、`提交答案`、`SPJ` 等

**操作指令**：
| 指令 | 说明 |
|------|------|
| `+标签名` | 添加标签 |
| `-标签名` | 移除标签 |
| `done` | 确认标签，进入下一步 |
| `skip` | 跳过当前步骤 |
| `随机` / `random` | 随机选题 |
| `看图` / `截图` | 查看当前题目的题面截图 |
| `back` | 返回上一步 |
| `quit` | 退出 |

**题面查看流程**：
1. 选题后先发送**题目摘要**（标题、难度、链接）
2. 再发送**题面文本摘要**，便于先快速阅读
3. 用户输入「看图」或「截图」后，Bot 发送**洛谷题面截图**

> 当前版本已临时关闭 Markdown 长图渲染，`看图` 与 `截图` 都会走更稳定的网页截图路径。

**示例**：
```
筛选条件：难度=5（提高+/省选−）+ 上海（区域）+ 2020（时间）+ O2优化（特殊题目）
→ 找到 4 道题
→ 第 1 题：P9821 [ICPC 2020 Shanghai R] Sum of Log
```

## 技术亮点

- **Retina 级别截图** — `device_scale_factor=2`，物理分辨率翻倍，文字图表清晰锐利
- **智能缓存** — 登录时自动缓存个人数据，支持 `-f` 强制刷新
- **验证码容错** — 识别失败后自动重试（最多 5 次），JS 方式关闭弹窗不卡死
- **异步架构** — Playwright 在线程池中运行，不阻塞 AstrBot 事件循环
- **UI 交互筛选** — 支持区域/时间等必须点击选择的标签
- **文本预览 + 网页截图** — 先看摘要，再按需发送稳定的题面截图

## 项目结构

```
astrbot_plugin_luogu/
├── main.py                 # 插件主入口
├── requirements.txt        # 依赖列表
├── metadata.yaml           # 插件元数据
├── luogu/
│   ├── __init__.py
│   ├── data_fetcher.py    # 数据提取 + 截图模块
│   ├── chart_generator.py # 图表生成模块
│   ├── screenshot.py      # 网页截图（备用）
│   ├── problem_fetcher.py # 题库筛选与跳转模块
│   ├── tags.py           # 标签常量与模糊匹配
│   └── models.py          # 数据模型
├── cookies/               # 登录 Cookie（不提交到 Git）
└── user_data/             # 用户绑定数据（不提交到 Git）
```

## 注意事项

1. **隐私保护**: Cookie 文件包含敏感登录信息，已加入 `.gitignore`，请勿提交到 Git
2. **验证码**: 首次登录可能需要验证码，ddddocr 可自动识别；未安装则需手动输入
3. **Cookie 有效期**: 洛谷 Cookie 有有效期限制，长时间不使用可能需要重新绑定
4. **推荐私聊使用**: `/luogu bind` 包含密码，请在私聊中发送避免泄露

## 更新日志

### v1.2.0 (2026-03-27)
- 普通聊天中的 `luogu_problem_search` 现在会记住上一轮筛选条件，可继续处理“总共有多少道”“随便来一道”“第 3 题”“再加个线段树标签”等追问
- 普通聊天里选出题目后，也可以继续说“把这道题转发出来”“看图”“截图”，不再要求必须切回 `/luogu jump`
- 普通聊天候选结果文案会明确区分“总题数”和“当前只展示前 N 道候选”，避免把展示数量误当成总数
- `/luogu jump` 仍然保留完整的显式多轮步骤控制；`back` 这类回退操作继续建议走 `/luogu jump`

### dev 分支联调清单
- 用普通聊天先测首轮选题：`四叶，跳一道洛谷线段树紫题`
- 紧接着测试追问总数：`总共有多少道`
- 测试继续缩小条件：`再加个后缀自动机标签`
- 测试随机与序号选题：`随便来一道`、`第 3 题`
- 选中题目后测试题面能力：`把这道题转发出来`、`看图`、`截图`
- 观察运行日志，确认普通聊天链路里首个工具调用始终是 `luogu_problem_search`，而不是 `web_search` / `fetch_url` / `astrbot_execute_python`

### v1.1.0 (2026-03-26)
- 新增 `/luogu jump` 题库跳转功能
- 支持难度筛选（0-7 级难度）
- 支持标签筛选（算法/来源/时间/区域/特殊题目）
- 支持模糊匹配标签名称
- UI 交互支持区域/时间标签（必须点击选择）
- Markdown 先发+点击渲染题面图片
- 新增 `_render_and_send_screenshot()` 异步截图渲染

### v1.0.0 (2026-03-25)
- 正式稳定版发布
- 支持 6 条核心指令 + `-f` 强制刷新
- Retina 级别高清截图
- 验证码识别自动重试机制

## License

MIT License
