① “session 情景丢失了吗？”
大概率不是 session 丢失，而是第二次“图也来”没有触发工具链。

从日志看：

15:33:48 已记录：remembered selected problem: pid=P1600（session 里有当前题）
15:34:09 用户说“图也来”后，没有出现 [Luogu LLM] enforced luogu toolset...、Agent 使用工具: ['luogu_problem_image'] 这类日志
只出现了普通回复 “稍等，我截个图给你”，随后也没有截图工具执行结果
=> 说明是意图路由/工具放行没命中，不是 session 本身没了。
（和前一轮“看看图”不同：那次虽然有 400 噪音，但后续确实调用了 luogu_problem_image 并成功发图。）

② “操作还是太慢”
你这个感受是对的，慢点主要在：

ProblemFetcher 的网页链路（打开页面 + 等待 + 抓取）
_detect_page_size 检测和深页定位流程
“随机/选题后自动接题面”的多工具串行（search -> statement）带来的额外耗时
有时还叠加引用图 400 错误的额外处理噪音
1) workspace 分支关键结构与职责（核心部分）
仓库（origin/workspace）核心文件：

/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/main.py

插件主入口、LLM 工具路由、会话管理
关键段：
_message_has_quoted_image_context（约 1997）
_sanitize_quoted_image_request（约 2097）
on_llm_request（约 2427）
/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/luogu/problem_lookup.py

普通聊天筛题的语义判断与上下文合并
关键函数：
should_merge_luogu_lookup_context（198）
should_start_new_luogu_lookup（228）
merge_luogu_lookup_context（254）
/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/luogu/problem_fetcher.py

Playwright 题库访问、分页、选题跳转、截图
关键函数：
_detect_page_size（208）
navigate_to_problem（251）
2) 主流程（普通聊天选题链路）
main.py:on_llm_request
→ 决定是否强制放行 luogu 工具（search/statement/image）
→ luogu_problem_search（内部到 problem_lookup.py）
→ problem_lookup.py 决策“新查询还是继承上下文”
→ 调 problem_fetcher.py 做网页筛题/选题
→ 回写 session（current_pid、list_url、tags、difficulty 等）
→ 若策略允许再调用 statement/image。

3) 对照 last test.txt 的逐条诊断
事件A：看看图（15:32:07）
出现 400 unknown variant image_url（Core 层）
但随后插件照常触发 luogu_problem_image，并成功截图 P1685
结论：这是“引用图兼容性噪音 + 插件后续容错成功”，不是功能完全失败。

事件B：再另外来一道LCA紫题 + 来第一道
从日志看筛题与选题都正确（tag=211、选中 P1600）
结论：这段会话是通的，session 正常更新。

事件C：图也来（15:34:09）
未看到 luogu 工具强制放行日志，也未看到 luogu_problem_image 执行日志
只发了自然语言“稍等，我截个图给你”，最终未发图
根因推断（高置信）：
该表达“图也来”没有稳定命中 follow-up image 意图，导致没走工具链。
不是 session 丢失，是“路由没打到工具”。

4) 下一步修改建议（最小改动，按优先级）
P0（必须先做）
P0-1：修复“图也来/图呢”类追问漏触发工具
目标：凡是“当前 session 已有 current_pid”且用户在追问图，必须稳进 luogu_problem_image。
文件：/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/main.py
改动点：

扩充 follow-up image 词表（如“图也来”“图呢”“来图”“上图”“发图”）
增加兜底规则：
若存在 luogu session 且 current_pid 有值
且消息是短追问（如“图呢”）/含图意图关键词
→ 强制 tool_names = ['luogu_problem_image'] 副作用：可能把极少数非洛谷“图”请求误判为洛谷图。
缓解：只有“有 luogu current_pid”才启用兜底。
验证：复测 题面发过去了 -> 图也来 -> 图呢，确认每次都有工具执行日志。
P0-2：优化“引用图”兼容噪音处理说明（插件侧可控部分）
目标：减少用户感知到的 400 噪音，至少让行为可预期。
文件：/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/main.py
改动点：

在检测到引用图片上下文时，统一走“文本追问 + 工具执行”路径，并附简短提示（避免误以为失败）
保持 _sanitize_quoted_image_request 兜底清理 说明：日志中的 400 发生在 Core 更早阶段，插件无法完全根治，但可降低后续混乱感。
P1（紧跟）
P1-1：减少不必要的自动串行工具调用（提速最直接）
目标：降低单轮响应时间。
文件：/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/main.py
改动点：

对“随机来一道/来第一道”默认只执行 luogu_problem_search，不要默认再接 statement
用户明确说“发题面/看题面”再触发 statement 收益：可减少一次完整网页抓取/题面获取开销。
副作用：默认少一步“自动贴题面”，交互风格略变化。
验证：对比同一请求前后耗时（日志时间戳）。
P1-2：分页探测复用（轻量）
文件：/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/luogu/problem_fetcher.py
改动点：

navigate_to_problem 优先使用 page_size_hint
仅在必要时再 _detect_page_size，避免重复检测 验证：观察 "_detect_page_size 第X次"日志次数下降。
P2（质量增强）
P2-1：增强上下文决策可观测性
文件：/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/luogu/problem_lookup.py
改动点：

在 should_start_new... / should_merge... / merge... 打结构化日志（decision + reason） 价值：以后定位“串味/误继承”会非常快。