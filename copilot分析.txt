插件主流程已经比较完整，核心功能可用。
• 最新测试里真正稳定复现的问题主要有 2 类：
i. 引用图片导致 provider 400（image_url vs text）：发生在 AstrBot Core 层，插件层只能做兜底和降噪。
ii. LLM 工具参数 query 经常没传进去：日志里反复出现 参数：{}，目前靠 event.message_str fallback 扛住，属于“能跑但不稳”。
• 你担心的“串题（旧题面串到新筛选）”在当前这版代码里已做了清理（_remember_luogu_lookup 在 candidate-only 场景会 pop current_pid/current_title/current_md），并且 last test.txt 里“线段树第一题”也确实落到了 P1186，说明这个点当前已明显改善。


2) 基于 last test.txt 的真实行为分析
A. 正常链路（通过）
“来一道LCA紫题” → 返回 50 道候选（正常）
“随便来一道” → 选中 P4074，题面发送成功（正常）
“图片也发过来” → 题图发送成功（正常）
“再找线段树紫题” → 返回 350 道候选（正常）
“第一题” → 成功选中 P1186，题面发送成功（正常）
“看图” → P1186 截图发送成功（正常）
B. 反复出现的问题（高价值）
Core 报错：引用图片失败
日志关键字：
core.astr_main_agent:574
unknown variant image_url, expected text
说明：引用链里含图片 part，provider 是 text-only，不接受 image_url。
特征：报错后插件链路仍继续执行，多数场景不致命，但会污染日志、影响稳定性观感。
tool 参数丢失
日志中多次出现：
使用工具：luogu_problem_search，参数：{}
随后 main.py 打印 recovered missing query from event.message_str
说明：工具签名与 runner 参数注入机制存在兼容缝隙，当前靠 fallback 顶住。
工具调度偏“保守冗余”
默认经常放开 search + statement，导致在只有候选时也会尝试 statement，然后返回“还没选中题目”提示。
功能上没坏，但会增加一次无效工具调用和一点延迟。
3) 文档/元数据一致性问题（应尽快收口）
README.md 写的是更新日志 v1.2.0；
metadata.yaml 还是 version: v0.2.0，author/repo 也是模板值（YourName / yourname）。
这会直接影响发布识别与安装端感知，建议尽快同步。
4) 下一步修改建议（按优先级）
P0（先做，影响稳定性与可维护性）
加“引用图片失败”插件侧降级提示（非根治，但能兜住体验）
文件：/home/runner/work/astrbot-luogu-fetcher/astrbot-luogu-fetcher/astrbot-luogu-fetcher/main.py（on_llm_request 附近）
建议：

检测消息是否“引用+图片语义”，在 text-only provider 场景给出明确提示：“请不要引用图片，直接文本重发需求”。
保持 luogu tool 路由照常进行，避免用户误判为插件崩了。
备注：根治要在 AstrBot Core 做 image part 过滤。
收紧默认工具放行策略，减少无效 statement 调用
位置：on_llm_request 的 tool_names 分支
建议：

首轮普通筛题只放 luogu_problem_search；
只有“明确选题/随机/给定PID/显式题面诉求且已有 current_pid”才放 statement。
收益：降低延迟和噪音，日志更干净。
P1（本周建议完成）
修复/增强 query 参数注入兼容性
位置：main.py 内 @filter.llm_tool(name="luogu_problem_search")
建议：

评估改为更显式的参数 schema（或按 AstrBot 推荐写法补全 tool 参数定义），减少 参数：{} 发生率；
fallback 保留，作为兜底。
目标：日志里不再频繁靠 recovered missing query。
给 luogu 普通聊天 session 增加 TTL
位置：_remember_luogu_lookup / _resolve_luogu_target_pid
建议：

记录 updated_at，超时（如 10~20 分钟）后要求重新选题。
收益：减少跨长会话的上下文污染概率。
P2（优化项）
减少 current_md 长文本驻留
建议按 PID + 短 TTL 缓存，或不长期放 session。
增强结构化日志
每次题面/题图输出前打印 resolved_pid 与来源（explicit/session）。
5) 建议你下一轮实际改动顺序（最小变更路径）
main.py：先改 tool 路由收敛（P0-2）
main.py：加引用图片的降级提示（P0-1）
metadata.yaml + README.md：版本与 repo/author 对齐
再跑你现有回归用例